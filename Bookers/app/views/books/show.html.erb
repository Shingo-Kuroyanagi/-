<div>
  <% if @book.errors.any? %>
  <%=  @book.errors.count %> errors prohibited this book from being saved
 <ul>
  <% @book.errors.full_messages.each do |message| %>
  <li><%= message %></li>
 <% end %>
 </ul>
 <%end %>
</div>
 
 <%= render 'users/list', book: @book user: @user %>
<div class="notice"><%= notice%></div>

<h4>Book detail<h4>
<table class="table table-hover">
  
 <tbody>
    <tr>
     <td>
       <%= attachment_image_tag @user,:profile_image_id,:fill,60,60,format: 'jpeg',fallback: "no_image.jpg", size: '60x60'%>
     </td>
     <td>
  　   <%=@book.title %>
     </td>
     <td>
  　   <%=@book.body %>
     </td>
     <if current_user.id ! == @book.user_id %>
     <td>
      <%= link_to 'Edit',edit_book_path(book.id)、class: "btn btn-success" %>
      <%= link_to 'Destroy',books_path, notice: "Book was succesfully destorted", method: :delete,"data-confirm" =>"本当に消しますか?" class: "btn btn-danger"%>
      </td>
    </tr>
  　 <% end %>
  </tbody> 
  </table>
  </div>
 